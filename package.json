{
  "name": "oxlint-annotation-repro",
  "version": "0.0.1",
  "private": false,
  "description": "Minimal repro for oxlint annotations and PR comments",
  "type": "module",
  "scripts": {
    "lint": "oxlint -f unix src/**/*.ts"
  },
  "devDependencies": {
    "oxlint": "^1.36.0"
  }
}
